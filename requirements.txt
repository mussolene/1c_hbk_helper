# Core dependencies (from pyproject.toml). For reproducible builds use:
#   pip install -e . && pip freeze > requirements.lock
# Or: pip-compile pyproject.toml -o requirements.txt

flask>=3.0
beautifulsoup4>=4.12
qdrant-client>=1.7
