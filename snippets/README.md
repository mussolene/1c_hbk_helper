# Реальные сниппеты (загружаются в память MCP)

Положите сюда файлы `*.bsl`, `*.1c`, `*.md` или `snippets.json`.

Эта папка монтируется в контейнер (read-only), при старте выполняется `load-snippets`.

Примеры форматов — в `docs/snippets/`.
