// Пример: добавление реквизита и элемента на форму
// Формат файла для загрузки из папки (SNIPPETS_DIR): *.bsl, *.1c

НовыйРеквизит = Новый РеквизитФормы("МойРеквизит", Новый ОписаниеТипов("Строка"), , "Заголовок", Ложь);
ИзменитьРеквизиты(Новый Массив(НовыйРеквизит), Неопределено);

НовыйЭлемент = Элементы.Добавить("МойРеквизит", Тип("ПолеФормы"));
НовыйЭлемент.ПутьКДанным = "МойРеквизит";
НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
