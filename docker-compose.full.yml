# Full mode: один контейнер mcp — API + ingest + cron + load-snippets + watchdog.
# usage: docker compose -f docker-compose.base.yml -f docker-compose.full.yml up -d   или   make up-full
#
# full: mcp runs ingest, status in shared SQLite /app/var/ingest_cache
services:
  mcp:
    environment:
      MCP_MODE: full
